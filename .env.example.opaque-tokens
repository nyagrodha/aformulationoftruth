# Opaque Resume Token Configuration
# Copy this to your .env file and generate secure secrets

# JWT Secret (256 bits minimum)
# Used for signing JWTs that contain email_hash + session_id
# Generate with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_here_256_bits_minimum

# Resume Token Secret (512 bits recommended)
# Used for HMAC hashing of opaque resume tokens
# The HMAC hash becomes the session_id (database primary key)
# Generate with: openssl rand -hex 64
RESUME_TOKEN_SECRET=your_resume_token_secret_here_512_bits_minimum

# Example generation commands:
# JWT_SECRET=$(openssl rand -hex 32)
# RESUME_TOKEN_SECRET=$(openssl rand -hex 64)

# Or using Deno:
# deno eval "console.log(Array.from(crypto.getRandomValues(new Uint8Array(32))).map(b => b.toString(16).padStart(2, '0')).join(''))"
# deno eval "console.log(Array.from(crypto.getRandomValues(new Uint8Array(64))).map(b => b.toString(16).padStart(2, '0')).join(''))"
