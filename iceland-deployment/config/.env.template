# Gimbal Storage API Environment Configuration
# Copy this file to .env and fill in the values

# ============================================================================
# Database Configuration
# ============================================================================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://gimbal:CHANGE_ME@localhost:5432/gimbal_storage

# ============================================================================
# API Authentication
# ============================================================================
# API key for Bearer token authentication
# Generate with: openssl rand -hex 32
API_KEY=CHANGE_ME_64_HEX_CHARACTERS

# ============================================================================
# Encryption Configuration
# ============================================================================
# AES-256-GCM encryption key (must be exactly 64 hex characters = 32 bytes)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=CHANGE_ME_64_HEX_CHARACTERS

# ============================================================================
# Server Configuration
# ============================================================================
# Environment mode (development | production)
NODE_ENV=production

# Port for API server (internal, behind Caddy reverse proxy)
PORT=3001

# Host to bind to
HOST=0.0.0.0

# Domain name
DOMAIN=gimbal.fobdongle.com

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
# 1. Never commit this file with real credentials
# 2. The API_KEY and ENCRYPTION_KEY must match on proust server
# 3. Use strong random keys - never use predictable values
# 4. Restrict file permissions: chmod 600 .env
# 5. Backup this file securely after deployment
