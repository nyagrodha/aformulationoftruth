# Gimbal Storage API Configuration
# Runs on 185.144.234.146 (Iceland VPS)

# Server Configuration
PORT=6789
HOST=0.0.0.0
NODE_ENV=production

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=a4m_db
DB_USER=a4m_app
DB_PASSWORD=jsT@sA2nd1nsd3cl2y0

# Security
API_KEY=change_this_to_match_main_server_vps_api_key
ENCRYPTION_KEY=change_this_to_match_main_server_vps_encryption_key

# Note: API_KEY and ENCRYPTION_KEY must match the values in
# the main server's .env file (VPS_API_KEY and VPS_ENCRYPTION_KEY)
