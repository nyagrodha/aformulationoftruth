{
  "name": "aformulationoftruth-site",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "npm-run-all build:backend build:frontend",
    "build:backend": "tsc -p apps/backend/tsconfig.json",
    "build:frontend": "vite build -c apps/frontend/vite.config.ts"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "node-ipinfo": "^4.1.0",
    "typescript": "^5.9.2"
  }
}
